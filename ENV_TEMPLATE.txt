# ──────────────────────────────────────────────────────────────────────────────
# Environment configuration template
# ──────────────────────────────────────────────────────────────────────────────
# Copy the relevant sections into:
#   • .env.development.local  (dev project credentials)
#   • .env.production.local   (prod project credentials)
# Vite will automatically pick the correct file based on the command you run.
# Never commit real keys back to the repo.
# ──────────────────────────────────────────────────────────────────────────────

# --- Shared config ------------------------------------------------------------
# If you re-enable OpenAI, add your project key here (not required for Supabase)
# VITE_OPENAI_API_KEY=sk-...

# --- Supabase (Frontend) ------------------------------------------------------
# Get these from Supabase → Project → Settings → API
# Development project
VITE_SUPABASE_URL=https://YOUR_DEV_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=dev-anon-key

# Production project
# VITE_SUPABASE_URL=https://YOUR_PROD_PROJECT.supabase.co
# VITE_SUPABASE_ANON_KEY=prod-anon-key

# --- Supabase (Service role for scripts) --------------------------------------
# Only needed for Node scripts (importSimulations, deactivateSimulations, etc.)
# export SUPABASE_URL=https://YOUR_DEV_PROJECT.supabase.co
# export SUPABASE_SERVICE_ROLE_KEY=dev-service-role

# export SUPABASE_URL=https://YOUR_PROD_PROJECT.supabase.co
# export SUPABASE_SERVICE_ROLE_KEY=prod-service-role


